/**
 * External destinations the upsell explainers link to. Kept in one place so the
 * Pro URL never drifts across the lock points (workflow palette, connectors).
 * These are plain anchor links the user clicks, not runtime service calls.
 */
export const PRO_UPGRADE_URL = "https://flexatech.com/formflow/pro";
